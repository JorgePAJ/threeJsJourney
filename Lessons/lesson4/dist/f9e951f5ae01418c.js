const scene=new THREE.Scene,geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:"#e3bfd4"}),mesh=new THREE.Mesh(geometry,material);scene.add(mesh);const sizes={widht:800,height:600},camera=new THREE.PerspectiveCamera(75,sizes.widht/sizes.height);scene.add(camera),camera.position.z=3;const canvas=document.querySelector("canvas.webgl"),renderer=new THREE.WebGLRenderer({canvas});renderer.setSize(sizes.widht,sizes.height),renderer.render(scene,camera);